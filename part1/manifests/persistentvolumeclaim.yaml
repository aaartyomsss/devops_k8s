apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc-claim
  namespace: small-apps
spec:
  storageClassName: sample-persistent-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
